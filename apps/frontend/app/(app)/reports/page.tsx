import { ComingSoonPage } from "@/components/shared/ComingSoonPage";

export default function ReportsPage() {
  return (
    <ComingSoonPage
      module="Reports"
      phase={3}
      description="On-demand report generation — Sales, Lead, Dealer, Marketing, Pipeline, Customer, Product and Warranty reports with CSV/Excel/PDF export."
    />
  );
}
