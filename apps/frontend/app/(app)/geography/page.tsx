import { ComingSoonPage } from "@/components/shared/ComingSoonPage";

export default function GeographyPage() {
  return (
    <ComingSoonPage
      module="Geography"
      phase={3}
      description="India-focused geographic intelligence — revenue/orders/leads/demand by state with a State → City → Dealer → Sales drill-down."
    />
  );
}
