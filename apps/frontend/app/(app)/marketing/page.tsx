import { ComingSoonPage } from "@/components/shared/ComingSoonPage";

export default function MarketingPage() {
  return (
    <ComingSoonPage
      module="Marketing"
      phase={2}
      description="Spend, CPL, ROAS/ROI tracking across Google Ads, Meta Ads, Website, WhatsApp, Organic and other channels."
    />
  );
}
