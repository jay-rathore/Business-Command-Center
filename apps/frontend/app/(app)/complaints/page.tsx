import { ComingSoonPage } from "@/components/shared/ComingSoonPage";

export default function ComplaintsPage() {
  return (
    <ComingSoonPage
      module="Complaints"
      phase={3}
      description="Full complaint lifecycle (Open → Closed) with priority, assignment, due dates and average resolution time."
    />
  );
}
