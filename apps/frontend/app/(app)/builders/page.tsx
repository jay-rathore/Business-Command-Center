import { ComingSoonPage } from "@/components/shared/ComingSoonPage";

export default function BuildersPage() {
  return (
    <ComingSoonPage
      module="Builders"
      phase={2}
      description="Builder directory with the same tracked fields and rankings as Architects, tuned to project-value influence."
    />
  );
}
