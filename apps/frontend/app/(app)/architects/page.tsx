import { ComingSoonPage } from "@/components/shared/ComingSoonPage";

export default function ArchitectsPage() {
  return (
    <ComingSoonPage
      module="Architects"
      phase={2}
      description="Architect directory with projects, referrals, leads, sample requests and revenue influenced."
    />
  );
}
